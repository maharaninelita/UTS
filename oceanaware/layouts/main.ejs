<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | OceanAware</title>
    <style>
        /* Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body styling */
        body {
            font-family: Arial, sans-serif;
            color: #ffffff;
            background-color: #0a0f1e;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
        }

        /* Header */
        h1 {
            font-size: 2.5em;
            color: #00a8ff;
            text-shadow: 0px 0px 10px #00a8ff, 0px 0px 20px #1e90ff;
            margin-bottom: 20px;
            text-align: center;
        }

        /* Navigation */
        nav ul {
            list-style-type: none;
            display: flex;
            gap: 20px;
            margin-bottom: 40px;
            padding: 10px 20px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            backdrop-filter: blur(10px);
        }

        nav ul li a {
            color: #00a8ff;
            text-decoration: none;
            font-size: 1.2em;
            padding: 10px 20px;
            transition: all 0.3s ease;
            border-radius: 8px;
        }

        nav ul li a:hover {
            color: #ffffff;
            background-color: #00a8ff;
            box-shadow: 0px 0px 10px #00a8ff;
        }

        /* Container */
        .container {
            width: 90%;
            max-width: 1200px;
            padding: 20px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            backdrop-filter: blur(10px);
            box-shadow: 0px 4px 10px rgba(0, 168, 255, 0.3);
        }

        /* Footer */
        footer {
            margin-top: 40px;
            font-size: 0.9em;
            color: rgba(255, 255, 255, 0.7);
        }
    </style>
</head>
<body>
    <h1>WELCOME TO OCEANAWARE</h1>
    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/users">Users</a></li>
            <li><a href="/volunteers">Volunteers</a></li>
            <li><a href="/eventDates">Event Dates</a></li>
            <li><a href="/locations">Locations</a></li>
            <li><a href="/login">Login</a></li>
        </ul>
    </nav>
    <div class="container">
        <!-- Content Goes Here -->
    </div>
    <footer>
        &copy; 2024 OceanAware. All rights reserved.
    </footer>
</body>
</html>
